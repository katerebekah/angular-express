extends layout

block body
  div.container(ng-controller='AppCtrl')
    h2(ng-show="name") Hello {{name}}
    div.container(ng-view)

    div
      | Angular seed app: v
      span(app-version)

    table.table
      tr
        td Hello
        td World
